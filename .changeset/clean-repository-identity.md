---
"@mivama/ui": patch
---

Point the published package metadata and release identity checks at the canonical `mivama-digital/ui` repository so npm provenance and Trusted Publishing use the current repository identity.
